package com.github.helendigger.taskapi.dto.validation;

/**
 * Marker annotation used in entity validation
 * Validates partial cycle, allows absent fields, so they aren't modified
 */
public interface TaskEditing {
}
